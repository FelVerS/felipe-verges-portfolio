{
  "name": "felipe-verges-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personal portfolio and CV website for Felipe Verges Suarez.",
  "scripts": {
    "test": "node --test scripts/validate.mjs",
    "check": "node --check src/js/main.js && node --check src/js/navigation.js && node --check src/js/reveal.js && node --test scripts/validate.mjs",
    "serve": "python -m http.server 4173 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
